/* enumberable type */

/* 상수를 그룹화해서 사용하고 싶을 때 enum */
enum Direction{
	UP, 
	DOWN, 
	LEFT, 
	RIGHT,
}

const direction = {
	up : Direction.UP,
	down : Direction.DOWN,
	left : Direction.LEFT,
	right : Direction.RIGHT,
}

enum End_point {
	USER = 'www.github.com/heroson02',
}